{
  "name": "mikado-coming-soon",
  "version": "1.0.0",
  "private": true,
  "description": "Page coming-soon MIKADO — build Tailwind CSS",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/style.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./assets/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
